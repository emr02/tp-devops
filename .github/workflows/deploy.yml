name: Deploy

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  deploy:
    if: github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-22.04

    steps:
      # Checkout the code
      - name: Checkout code
        uses: actions/checkout@v2.5.0

      # Log in to Docker Hub using the provided credentials
      - name: Login to DockerHub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      # Build the Docker image for the backend
      - name: Build backend image
        uses: docker/build-push-action@v3
        with:
          context: ./simple-api
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-simple-api:latest

      # Build the Docker image for the database
      - name: Build database image
        uses: docker/build-push-action@v3
        with:
          context: ./database
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-database:latest

      # Build the Docker image for the HTTP server
      - name: Build httpd image
        uses: docker/build-push-action@v3
        with:
          context: ./http-server
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-httpd:latest

      # Save Docker images as artifacts
      - name: Save Docker images as artifacts
        run: |
          docker save ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-simple-api:latest -o simple-api.tar
          docker save ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-database:latest -o database.tar
          docker save ${{ secrets.DOCKERHUB_USERNAME }}/tp-devops-httpd:latest -o http-server.tar
        uses: actions/upload-artifact@v3
        with:
          name: docker-images
          path: |
            simple-api.tar
            database.tar
            http-server.tar